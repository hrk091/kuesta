---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: ocdemoes.nwctl.hrk091.dev
spec:
  group: nwctl.hrk091.dev
  names:
    kind: OcDemo
    listKind: OcDemoList
    plural: ocdemoes
    singular: ocdemo
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OcDemo is the Schema for the ocdemoes API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: OcDemoSpec defines the desired state of OcDemo
            properties:
              address:
                type: string
              password:
                type: string
              port:
                type: integer
              rolloutRef:
                description: RolloutRef is the name of DeviceRollout to which this
                  device belongs.
                type: string
              username:
                type: string
            required:
            - rolloutRef
            type: object
          status:
            description: OcDemoStatus defines the observed state of OcDemo
            properties:
              checksum:
                description: Checksum is a hash to uniquely identify the entire device
                  config.
                type: string
            required:
            - checksum
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
