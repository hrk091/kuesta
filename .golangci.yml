linters:
  enable:
    - bodyclose
    - errorlint
    - exhaustive
    - exportloopref
    - gci
    - gocognit
    - goconst
    - godot
    - gofumpt
    - gomnd
    - makezero
    - nakedret
    - nestif
    - noctx
    - predeclared
    - testpackage
linters-settings:
  gocognit:
    # Minimal code complexity to report.
    # Default: 30 (but we recommend 10-20)
    min-complexity: 20
issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - gocognit
        - goconst
        - gomnd