apiVersion: v1
kind: Service
metadata:
  name: nwctl-server
spec:
  type: ClusterIP
  selector:
    app: nwctl-server
  ports:
    - name: grpc
      port: 9339
      targetPort: 9339
      protocol: TCP
